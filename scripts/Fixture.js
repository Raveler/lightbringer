define(["Compose", "Logger"], function(Compose, Logger) {
	
	// a fixture represents a certain obstacle used in physics - a circle or a box
	var Fixture = Compose(function() {

	},
	{
		getFixtureDef: function() {
			// to define
		}
	});

	return Fixture;
});